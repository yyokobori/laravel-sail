## 前提構成
- テストは都度作成する
- テストは「振る舞い」を検証し、実装詳細には依存しないこと

---

# バックエンド用（Laravel Featureテスト）

## 対象API
- ルート:
- HTTPメソッド:
- コントローラー:
- 処理概要:

## テスト要件
- 正常系
- バリデーションエラー
- 認証エラー（必要な場合）
- 権限エラー（必要な場合）
- データベース検証（assertDatabaseHas）
- HTTPステータスコードを明示的に検証
- JSON構造をassertJsonStructureで検証
- RefreshDatabaseトレイトを使用
- 可能な限りFactoryを使用
- ハードコード値を避ける
- 境界値テストも含める

※ APIはJSONレスポンスを返す前提。

---

# フロントエンド用（Playwright E2E）

## 対象画面
- URL:
- 主な機能:
- 関連API:

## テスト要件
- 正常操作フロー
- バリデーション表示確認
- API失敗時のエラーハンドリング確認
- 画面遷移確認
- テストは独立して実行可能にする
- data-testid または role セレクタを優先
- waitForTimeoutは禁止
- 非同期処理は await expect を使用
- UI変更に影響されにくい設計にする
- 境界値テストを含める

---

## 追加要求

- 現在の実装にテストしづらい構造がある場合は改善提案も含めること。